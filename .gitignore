# Python / Django
*.py[cod]
*$py.class
__pycache__/
*.so

# Ambientes Virtuais (Ajuste conforme o nome da sua pasta)
env/
venv/
.env
.venv/

# Variáveis de ambiente e segredos (NUNCA versionar!)
.env
.env.local
.env.production
# OBS: .env.example PODE ser versionado (é só o template)

# Banco de dados (se for usar SQLite local)
db.sqlite3
*.sqlite3

# Logs
*.log

# Arquivos de sistema
.DS_Store
Thumbs.db

implementação.txt

# Arquivos estáticos coletados (gerados pelo collectstatic)
staticfiles/

# Docker (opcional - alguns preferem versionar)
# Descomente as linhas abaixo se NÃO quiser versionar os arquivos Docker
# Dockerfile
# docker-compose.yml
# .dockerignore
